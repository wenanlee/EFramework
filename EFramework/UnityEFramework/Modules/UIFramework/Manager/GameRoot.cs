using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/// <summary>
/// 启动入口 GameRoot，需挂载在游戏物体上
/// </summary>
public class GameRoot : MonoBehaviour{
    void Start () {
		// this.Work().WaitTime(1.0f).AddWork(
  //           () => {  UIManager.Instance.ShowUI(UIPanelType.MainMenu); }
		// 	).Run();
       
    }
	
	void Update () {
		
	}
}
